import { Link, Route, Routes, useSearchParams } from 'react-router-dom';
function App() {
  return (
    <div>
      <h1> 도시에 대한 날씨</h1>
      <Routes>
        {/* <Route path="/" element={} /> */}
      </Routes>
    </div>
  );
}
